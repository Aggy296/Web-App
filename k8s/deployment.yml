apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
  namespace: dev

spec:
  replicas: 1

  selector:
    matchLabels:
      app: web-app

  template:
    metadata:
      labels:
        app: web-app

    spec:
      containers:
      - name: web-app

        image: ghcr.io/aggy296/web-app:674cda367c6dc3956d8c31d540d5e99aabc64409

        ports:
        - containerPort: 3000

        resources:
          requests:
            cpu: "10m"
            memory: "16Mi"
          limits:
            cpu: "50m"
            memory: "64Mi"

        envFrom:
        - configMapRef:
            name: web-app-config

        - secretRef:
            name: web-app-secret